/**
 * @description Obtener la última versión
 * @params {string} version - Establecer una versión en especifico
 */
export const getVersion = (version: string): void => {
  console.info("1.0.0");
};
